services:
  front-envoy:
    image: envoyproxy/envoy:v1.31.0
    volumes:
    - ./config-dynamic-config.yaml:/etc/config.yaml
    command:
      -c /etc/config.yaml -l trace
    ports:
      - "8888:8888"
      - "9901:9901"